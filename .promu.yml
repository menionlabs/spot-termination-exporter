go:
  version: 1.26
repository:
  path: https://github.com/menionlabs/spot-termination-exporter
build:
  prefix: .
  binaries:
      - name: spot-termination-exporter
        path: .

  flags: -a -tags netgo
  ldflags: |
    -s
    -X main.version.Version={{.Version}}
    -X main.version.Revision={{.Revision}}
    -X main.version.Branch={{.Branch}}
    -X main.version.BuildUser={{user}}@{{host}}
    -X main.version.BuildDate={{date "20160902-15:04:05"}}
tarball:
  prefix: .
crossbuild:
  platforms:
    - linux/amd64
    - linux/386
